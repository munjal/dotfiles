alias config='/usr/bin/git --git-dir=$HOME/.cfg/ --work-tree=$HOME'
alias brewup='brew update; brew upgrade; brew prune; brew cleanup; brew doctor'
alias vi="/usr/local/bin/vim"
alias diff="git difftool -y -w --diff-filter=ACMRTUXB"
alias fdiff="git diff --color | diff-so-fancy"

alias prettyjson='python -m json.tool'

alias dl="it2dl"

alias tmp="cd ~/projects/tmp"

alias config='/usr/local/bin/git --git-dir=$HOME/.cfg/ --work-tree=$HOME'

alias gitconfig='vim ~/.gitconfig'
alias gs='clear && git status'
alias c=clear
alias loga='git log --oneline --decorate'
alias log="git log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(yellow)<%an>%Creset'"
alias xc='open *.xcworkspace'
alias ac='appcode *.xcworkspace'
alias gp="git stash && git pull -r && git stash pop"

alias reload="source ~/.zshrc"

alias rwifi='networksetup -setairportpower en0 off && networksetup -setairportpower en0 on'
alias f="ag -g"
alias r=ag

alias p=ptpython
alias kube=kubectl

alias fz='find * -type f | fzf'
